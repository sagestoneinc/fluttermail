{
  "name": "sagestone-api",
  "version": "0.1.0",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "lint": "eslint . --ext .ts",
    "test": "jest",
    "openapi:emit": "nest build && npx @nestjs/swagger-cli plugin"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/swagger": "^7.0.0",
    "pg": "^8.11.0",
    "redis": "^4.6.7"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0"
  }
}
